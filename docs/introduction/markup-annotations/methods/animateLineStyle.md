Turns on the display of animated border of a space markup when selected.

### Syntax

```typescript
RxCore.animateLineStyle(onoff)
```

### Parameters

- `onoff`: **boolean** — Allow the animated border display. Use `true` to enable , or `false` to disable it.

### Returns

- **NA** — This method does not return a value.