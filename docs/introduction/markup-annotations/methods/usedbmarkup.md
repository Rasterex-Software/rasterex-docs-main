When using a different source for annotation storage like SharePoint or database, you can turn off standard loading of annoations and measurements from xml based rxml files.


### Syntax

```typescript
RxCore.usedbmarkup(onOff)
```

### Parameters

- `onOff`: **boolean** — `true` = on, `false` = off.

### Returns

- **NA** — This method does not return a value.